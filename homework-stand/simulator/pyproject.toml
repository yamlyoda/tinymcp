[project]
name = "stand-simulator"
version = "0.1.0"
description = "Генератор истории и живого трафика для инцидентного стенда"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "psycopg[binary]>=3.2.0",
]

[tool.uv]
package = false
